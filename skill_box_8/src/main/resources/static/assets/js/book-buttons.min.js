function postponedBook() {

    id = window.location.href.split('/').pop()

    $.ajax({
        url: `/api/user/postponed/${id}`,
        type: 'POST',
        dataType: 'json',
    }).then(function(data) {

    });
}